All requested changes have been implemented successfully, and the application builds without errors.

Here's a summary of the latest changes:
- `app/layout.tsx`:
    - Fixed global layout issue where pages were stretching behind the sidebar. 
    - Removed `flex` from `body` and set `main` to `ml-[200px]` with `flex flex-col` to properly offset content from the fixed sidebar while allowing children to fill the remaining space.
    - Added `overflow-x-hidden` to `body` to prevent horizontal scrolling.
- `app/page.tsx`:
    - Replaced redundant `main` tag with `div` and used `flex-1` to correctly participate in the layout's flex flow.
- `app/upload/page.tsx` & `app/caption-vote/page.tsx`:
    - Refined "logged-out" state: Linked the "log in" text directly to Google OAuth using a button styled as plain underlined text to blend into the sentence.
    - Layout Fix: Switched from `min-h-screen` to `flex-1` to ensure components respect the parent container's margin and do not overlap with the sidebar.
- `components/ImageGrid.tsx`:
    - Replaced `w-screen h-screen` with `inset-0` in the modal backdrop to avoid layout overflow.
- `components/Sidebar.tsx`:
    - Confirmed sidebar width is `200px` and set to `fixed`.
    - Removed `hover:bg-gray-600` from both `Link` components to disable the background color change on hover.
    - Added a new `Link` to `/upload` with a "Upload" text.
- `lib/pipeline.ts`:
    - Created utility functions (`generatePresignedUrl`, `uploadToS3`, `registerImage`, `generateCaptions`) to handle the image captioning pipeline.
- Build and Vercel Readiness:
    - Resolved ESLint errors including `any` types and escaped entities.
    - Verified environment variable usage for Supabase.
    - Confirmed project structure follows Next.js App Router conventions.
